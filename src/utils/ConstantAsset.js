import { Dimensions, useWindowDimensions } from "react-native"
const {height,width} = Dimensions.get('window')

export const ASSETS  = {
    primaryColor:"#003366",
    height:height,
    width:width

 }